<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="css/styles.css" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
</head>

<body>
    <div class="row" style=" height:100%; margin-top:150px">
        <div>
            <!-- <div style="background-color:rgb(49, 49, 49);padding: 5px;border-radius: 15px;"> -->
            <!-- <h2 style="text-align: center;color:white;font-family: 'Montserrat', sans-serif;">Select Package For
                <%= msisdn %>
            </h2> -->
            <form action="/dailypackage" id="dailypackage" method="POST">
                <div class="column">
                    <div class="card">
                        <h2 class="font">PREPAID</h2>
                        <h2 class="font">DAILY PLAN</h2>
                        <h3 class="font">RS 4/Day</h3>
                        <input type="hidden" name="daily" id="daily" value="jazz-sku-1-day">
                        <input type="hidden" name="msisdn" id="msisdn" value="<%= msisdn %>">
                        <input type="hidden" name="user_status" id="user_status" value="<%= user_status %>">
                        <% if (locals.network_type==1 || locals.network_type==2) { %>

                            <button class="btnNext" id="weekly" type="submit">
                                    <a style="text-decoration: none;color:white;font-size: 18px;">Confirm</a></button>

                            <% } %>
                                <% if (locals.network_type==3) { %>

                                    <button class="btnNext" style="border: 1px solid #999999; background-color: #cccccc;color: #666666;" id="monthly" type="submit" disabled>
                                        <a
                                            style="text-decoration: none;color:#666666;font-size: 18px;pointer-events: none;">Confirm</a></button>

                                    <% } %>
                    </div>
                </div>
            </form>
            <form action="/weeklypackage" id="weeklypackage" method="POST">
                <div class="column">
                    <div class="card">
                        <h2 class="font">PREPAID</h2>
                        <h2 class="font">WEEKLY PLAN</h2>
                        <h3 class="font">RS 25/Week</h3>
                        <input type="hidden" name="weekly" id="weekly" value="jazz-sku-7-days">
                        <input type="hidden" name="msisdn" id="msisdn" value="<%= msisdn %>">
                        <input type="hidden" name="user_status" id="user_status" value="<%= user_status %>">
                        <% if (locals.network_type==1 || locals.network_type==2) { %>

                            <button class="btnNext" id="weekly" type="submit">
                                    <a style="text-decoration: none;color:white;font-size: 18px;">Confirm</a></button>

                            <% } %>
                                <% if (locals.network_type==3) { %>

                                    <button class="btnNext" style="border: 1px solid #999999; background-color: #cccccc;color: #666666;" id="monthly" type="submit" disabled>
                                        <a
                                            style="text-decoration: none;color:#666666;font-size: 18px;pointer-events: none;">Confirm</a></button>
                                    <% } %>

                    </div>
                </div>
            </form>

            <form action="/monthlypackage" id="monthlypackage" method="POST">
                <div class="column">
                    <div class="card">
                        <h2 class="font">POSTPAID</h2>
                        <h2 class="font">MONTHLY PLAN</h2>
                        <h3 class="font">RS 75/Month</h3>
                        <input type="hidden" name="monthly" id="monthly" value="jazz-sku-1-month">
                        <input type="hidden" name="msisdn" id="msisdn" value="<%= msisdn %>">
                        <input type="hidden" name="user_status" id="user_status" value="<%= user_status %>">

                        <% if (locals.network_type==1 || locals.network_type==2) { %>

                            <button class="btnNext" style="border: 1px solid #999999; background-color: #cccccc;color: #666666;" id="monthly" type="submit" disabled>
                                <a
                                    style="text-decoration: none;color:#666666;font-size: 18px;pointer-events: none;">Confirm</a></button>
                            <% } %>
                                <% if (locals.network_type==3) { %>

                                    <button class="btnNext" id="weekly" type="submit">
                                        <a style="text-decoration: none;color:white;font-size: 18px;">Confirm</a></button>

                                    <% } %>


                    </div>
                </div>
            </form>

        </div>

    </div>
    <script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>

    <% if (locals.success) { %>

        <script>
            Swal.fire({
                position: 'center',
                icon: 'success',
                html: "<b><%= success %></b>",
                customClass: 'swal-wide',
                showConfirmationButton: true,
                timer: 2500
            })
        </script>

        <% } %>
</body>

</html>